<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC   "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
   <mapper namespace="kr.co.gf.member.dao.MemberDAO">
   <insert id="join" parameterType="member">
   		INSERT INTO member(
   			mem_no, emp_no, ticket_no, name, birth, gender, phone, locker, gym_wear,
   			reg_date, branch, pt_chk
   		)
   			VALUES(
   				#{mem_no},#{emp_no},#{ticket_no},#{name},#{birth},#{gender},#{phone},
   				#{locker},#{gym_wear},#{reg_date},#{branch},#{pt_chk}
   			)
   </insert>
   
   </mapper>